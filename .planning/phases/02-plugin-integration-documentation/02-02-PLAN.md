---
phase: 02-plugin-integration-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "A visitor to the GitHub repo can read README.md and understand what this marketplace is"
    - "README contains exact CLI commands for adding the marketplace and installing plugins"
    - "README includes a plugin table showing available plugins with name and description"
  artifacts:
    - path: "README.md"
      provides: "Marketplace documentation for users"
      contains: "claude plugin marketplace add"
  key_links:
    - from: "README.md"
      to: "Djarvur/cc-mplace"
      via: "CLI command reference"
      pattern: "claude plugin marketplace add Djarvur/cc-mplace"
---

## Phase Goal

**As a** Claude Code user, **I want to** read clear documentation about the Djarvur marketplace, **so that** I can understand what it offers and how to start using plugins.

<objective>
Write a minimal README.md (~30-50 lines per D-05) that explains what the marketplace is, how to add it via CLI, and how to install plugins. Include a plugin table per D-06.

Purpose: Users arriving at the GitHub repo immediately understand the marketplace purpose and can start using it without reading source code.
Output: README.md with marketplace description, CLI commands, and plugin table.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/cc-mplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/cc-mplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-plugin-integration-documentation/02-CONTEXT.md
@.planning/phases/02-plugin-integration-documentation/02-RESEARCH.md
@.planning/phases/01-marketplace-index-schema/01-01-SUMMARY.md
</context>

<interfaces>
<!-- No code interfaces -- this plan creates a static Markdown file. -->

From CONTEXT.md locked decisions for README scope:

- D-05: Minimal README (~30-50 lines) -- essentials only: what this is, how to add marketplace, how to install plugins
- D-06: Include a plugin table (name + description) -- lets users browse before installing

CLI commands for README (from RESEARCH.md Code Examples):

```
claude plugin marketplace add Djarvur/cc-mplace
claude plugin install cc-websearch
claude plugin list
```

Plugin metadata for the table (from cc-websearch plugin.json per D-01):

- name: cc-websearch
- description: DDG-powered WebSearch and WebFetch replacement for Claude Code
  </interfaces>

<tasks>

<task id="02-02-01" type="execute">
<name>Task 1: Write README.md with marketplace docs and plugin table</name>
<description>Create README.md documenting the marketplace purpose, CLI commands for adding the marketplace and installing plugins, and a table of available plugins.</description>
<read_first>
- /Users/nil/DiskD/W/Djarvur/cc-mplace/.planning/phases/02-plugin-integration-documentation/02-CONTEXT.md (D-05, D-06 for README scope)
- /Users/nil/DiskD/W/Djarvur/cc-mplace/.planning/phases/02-plugin-integration-documentation/02-RESEARCH.md (Code Examples section for CLI commands)
- /Users/nil/DiskD/W/Djarvur/cc-websearch/.claude-plugin/plugin.json (for plugin metadata in the table)
- /Users/nil/DiskD/W/Djarvur/cc-mplace/CLAUDE.md (project description and core value for README intro)
</read_first>
<action>
Create `README.md` at the repo root. Target ~30-50 lines (per D-05). Structure:

1. **Title + one-line description:** "Claude Code Plugin Marketplace" -- what this repo is: a static plugin registry for Claude Code.

2. **What is this?** (2-3 sentences): Explain that this is a curated marketplace for Claude Code plugins. Users add it via CLI and install plugins with `claude plugin install`. Core value: discover and install plugins without manual cloning or config editing.

3. **Quick Start section:**
   - Command to add the marketplace: `claude plugin marketplace add Djarvur/cc-mplace`
   - Command to install a plugin: `claude plugin install <plugin-name>`
   - Command to list installed plugins: `claude plugin list`
   - Use fenced code blocks with `bash` syntax highlighting for each command.

4. **Available Plugins table** (per D-06): Markdown table with columns "Plugin" and "Description". One row for cc-websearch:
   - Plugin column: `cc-websearch`
   - Description column: "DDG-powered WebSearch and WebFetch replacement for Claude Code" (mirrored from plugin.json per D-01)

5. **License / footer line** if applicable, or omit if no license file exists.

Do NOT include: CONTRIBUTING.md content (Phase 3 concern), architecture details, development setup instructions, or anything beyond the essential user-facing documentation per D-05.
</action>
<acceptance_criteria>

- File `README.md` exists at repo root
- `grep -c 'claude plugin marketplace add Djarvur/cc-mplace' README.md` returns >= 1
- `grep -c 'claude plugin install' README.md` returns >= 1
- `grep -c 'cc-websearch' README.md` returns >= 1
- `grep -c '|' README.md` returns >= 2 (plugin table has header + separator + row)
- Line count is between 25 and 60 lines: `wc -l README.md` shows 25-60
- No CONTRIBUTING content: `grep -ci 'contributing' README.md` returns 0
  </acceptance_criteria>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                 |
| ----------------- | ------------------------------------------- |
| README.md -> User | Static documentation; no executable content |

## STRIDE Threat Register

| Threat ID | Category  | Component                                 | Disposition | Mitigation Plan                                              |
| --------- | --------- | ----------------------------------------- | ----------- | ------------------------------------------------------------ |
| T-02-03   | Spoofing  | README could reference wrong CLI commands | mitigate    | Exact commands verified against official docs in RESEARCH.md |
| T-02-SC   | Tampering | npm installs                              | accept      | No new packages in this plan; documentation-only task        |

</threat_model>

<verification>
1. `grep 'claude plugin marketplace add Djarvur/cc-mplace' README.md` finds the add command
2. `grep 'claude plugin install' README.md` finds the install command
3. Plugin table present with cc-websearch entry
4. Line count within D-05 target range (~30-50 lines)
</verification>

<success_criteria>

- README.md explains what the marketplace is (DOCS-01)
- CLI commands for adding marketplace and installing plugins are present (MIDX-04, PLUG-02 documentation)
- Plugin table shows cc-websearch with description (D-06)
- README is minimal (~30-50 lines) per D-05
  </success_criteria>

<output>
Create `.planning/phases/02-plugin-integration-documentation/02-02-SUMMARY.md` when done
</output>
