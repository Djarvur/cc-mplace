---
phase: 02-plugin-integration-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/marketplace.json
  - tests/marketplace.test.js
autonomous: false
requirements:
  - PLUG-01
  - PLUG-02
  - MIDX-04

must_haves:
  truths:
    - "cc-websearch appears in marketplace.json with correct metadata from plugin.json"
    - "Plugin source uses url format pointing to https://github.com/Djarvur/cc-websearch.git"
    - "Existing validation tests (schema + uniqueness) pass with the new entry"
    - "claude plugin marketplace add Djarvur/cc-mplace succeeds and registers the marketplace"
    - "claude plugin install cc-websearch resolves the plugin"
  artifacts:
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace index with cc-websearch as first plugin entry"
      contains: "cc-websearch"
    - path: "tests/marketplace.test.js"
      provides: "Explicit cc-websearch entry validation test"
      contains: "cc-websearch"
  key_links:
    - from: ".claude-plugin/marketplace.json"
      to: "https://github.com/Djarvur/cc-websearch.git"
      via: "source.url field"
      pattern: "cc-websearch\\.git"
---

## Phase Goal

**As a** Claude Code user, **I want to** add the Djarvur marketplace and install the cc-websearch plugin, **so that** I can use web search capabilities directly within Claude Code.

<objective>
Add cc-websearch as the first plugin entry to marketplace.json with metadata mirrored from the plugin's own plugin.json manifest, then verify the end-to-end CLI flow works from marketplace add through plugin install.

Purpose: The marketplace becomes functional end-to-end -- users can discover and install a real plugin through the official CLI workflow.
Output: Updated marketplace.json with cc-websearch entry, enhanced test coverage, verified CLI flow.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/cc-mplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/cc-mplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-plugin-integration-documentation/02-CONTEXT.md
@.planning/phases/02-plugin-integration-documentation/02-RESEARCH.md
@.planning/phases/01-marketplace-index-schema/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From cc-websearch plugin manifest at /Users/nil/DiskD/W/Djarvur/cc-websearch/.claude-plugin/plugin.json:

```json
{
  "name": "cc-websearch",
  "displayName": "WebSearch",
  "version": "0.1.0",
  "description": "DDG-powered WebSearch and WebFetch replacement for Claude Code"
}
```

From current marketplace.json at .claude-plugin/marketplace.json:

```json
{
  "$schema": "https://json.schemastore.org/claude-code-marketplace.json",
  "name": "djarvur-plugin-marketplace",
  "description": "Curated plugins for Claude Code",
  "owner": { "name": "Djarvur" },
  "plugins": []
}
```

Target marketplace entry (from RESEARCH.md Code Examples, per D-01 through D-04):

```json
{
  "name": "cc-websearch",
  "displayName": "WebSearch",
  "version": "0.1.0",
  "description": "DDG-powered WebSearch and WebFetch replacement for Claude Code",
  "author": { "name": "Djarvur" },
  "category": "search",
  "source": {
    "source": "url",
    "url": "https://github.com/Djarvur/cc-websearch.git"
  }
}
```

</interfaces>

<tasks>

<task id="02-01-01" type="execute">
<name>Task 1: Add cc-websearch plugin entry to marketplace.json + validate</name>
<description>Add the cc-websearch entry to the plugins array in marketplace.json using metadata mirrored from the plugin's own plugin.json, then add an explicit test asserting the entry exists with correct fields.</description>
<read_first>
- /Users/nil/DiskD/W/Djarvur/cc-websearch/.claude-plugin/plugin.json
- /Users/nil/DiskD/W/Djarvur/cc-mplace/.claude-plugin/marketplace.json
- /Users/nil/DiskD/W/Djarvur/cc-mplace/tests/marketplace.test.js
- /Users/nil/DiskD/W/Djarvur/cc-mplace/.planning/phases/02-plugin-integration-documentation/02-RESEARCH.md (Code Examples section for exact entry JSON)
</read_first>
<action>
Add cc-websearch plugin entry to the `plugins` array in `.claude-plugin/marketplace.json`. The entry MUST contain exactly these fields per locked decisions D-01 through D-04:

- `name`: "cc-websearch" (mirrored from plugin.json per D-01)
- `displayName`: "WebSearch" (mirrored from plugin.json per D-01)
- `version`: "0.1.0" (mirrored from plugin.json per D-01)
- `description`: "DDG-powered WebSearch and WebFetch replacement for Claude Code" (mirrored from plugin.json per D-01)
- `author`: object with `name: "Djarvur"` (per D-04)
- `category`: "search" (per D-04)
- `source`: object with `source: "url"` and `url: "https://github.com/Djarvur/cc-websearch.git"` (per D-02, with .git suffix per Pitfall 4 in RESEARCH.md)
- NO `sha` field (per D-03 -- SHA pinning deferred to v2 META-01)

After updating marketplace.json, run `npm run format` to ensure Prettier consistency.

Add a new test case in `tests/marketplace.test.js` inside the existing `describe("marketplace.json")` block that explicitly verifies the cc-websearch entry:

- Assert `marketplace.plugins.length >= 1`
- Find the entry where `name === "cc-websearch"` and assert it exists
- Assert the entry has `displayName: "WebSearch"`, `version: "0.1.0"`, `description` containing "DDG-powered"
- Assert the entry has `source.source === "url"` and `source.url === "https://github.com/Djarvur/cc-websearch.git"`
- Assert the entry has `author.name === "Djarvur"` and `category === "search"`

This explicit test supplements the existing generic tests (which already cover schema conformance and uniqueness) and provides clear failure output if the entry drifts from the expected metadata.
</action>
<acceptance_criteria>

- `grep -c '"cc-websearch"' .claude-plugin/marketplace.json` returns count >= 1
- `grep '"source": "url"' .claude-plugin/marketplace.json` finds the source type
- `grep 'cc-websearch\\.git' .claude-plugin/marketplace.json` finds the URL with .git suffix
- `grep -c '"sha"' .claude-plugin/marketplace.json` returns 0 (no SHA field)
- `npm test` passes all tests including the new explicit cc-websearch metadata test
- `npm run format:check` passes (Prettier-compliant)
  </acceptance_criteria>
  </task>

<task id="02-01-02" type="checkpoint:human-verify" gate="blocking">
<name>Task 2: Verify E2E CLI flow -- marketplace add + plugin install</name>
<what-built>
The marketplace.json now contains a valid cc-websearch plugin entry. The automated tests (schema validation, uniqueness, explicit metadata check) all pass. The E2E CLI flow needs manual verification because it requires the live Claude Code CLI interacting with GitHub.
</what-built>
<how-to-verify>
1. Run `claude plugin marketplace add Djarvur/cc-mplace` -- this should succeed and register the marketplace. If it returns an error, capture the exact error message.

2. Run `claude plugin marketplace list` -- verify "djarvur-plugin-marketplace" or "Djarvur/cc-mplace" appears in the list.

3. Run `claude plugin install cc-websearch` -- this should resolve the plugin via the marketplace entry, clone the repo, and install it. If it returns an error, capture the exact error message.

4. If step 3 fails with an error related to skills/commands not loading (GitHub issue #38670), note the exact error text -- this is a known Claude Code platform bug, not a marketplace issue. Per D-08, document the error and proceed.

5. Run `claude plugin list` -- verify cc-websearch appears as installed.

6. After verification, run `claude plugin uninstall cc-websearch` and `claude plugin marketplace remove Djarvur/cc-mplace` to clean up the local state so it does not interfere with future development.
   </how-to-verify>
   <resume-signal>Type "approved" if the CLI flow works end-to-end, or describe any errors encountered. Per D-08, errors from issue #38670 are acceptable -- document them and proceed.</resume-signal>
   </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                  |
| ----------------------------------- | -------------------------------------------------------------------------------------------- |
| marketplace.json -> Claude Code CLI | Static JSON consumed by CLI; malformed entries could cause CLI errors                        |
| Source URL -> Plugin repo           | URL points to external repo; if repo is compromised, plugin install fetches compromised code |

## STRIDE Threat Register

| Threat ID | Category  | Component                     | Disposition | Mitigation Plan                                                             |
| --------- | --------- | ----------------------------- | ----------- | --------------------------------------------------------------------------- |
| T-02-01   | Tampering | marketplace.json plugin entry | mitigate    | AJV schema validation + explicit metadata test assert exact expected values |
| T-02-02   | Tampering | Source URL in plugin entry    | accept      | URL is hardcoded to known Djarvur repo; PR-based review in Phase 3 CI       |
| T-02-SC   | Tampering | npm installs                  | accept      | No new packages installed in this phase; all deps verified in Phase 1       |

</threat_model>

<verification>
1. `npm test` passes -- all existing tests (schema, required fields, plugin entries, uniqueness) plus new cc-websearch metadata test
2. `npm run format:check` passes
3. E2E CLI flow verified via checkpoint (Task 2)
</verification>

<success_criteria>

- marketplace.json contains cc-websearch entry with metadata exactly matching plugin.json (D-01)
- Source uses `{ "source": "url", "url": "https://github.com/Djarvur/cc-websearch.git" }` format (D-02)
- No SHA field present (D-03)
- Author and category fields included (D-04)
- All automated tests pass
- E2E CLI flow verified (D-07) or errors documented (D-08)
  </success_criteria>

<output>
Create `.planning/phases/02-plugin-integration-documentation/02-01-SUMMARY.md` when done
</output>
