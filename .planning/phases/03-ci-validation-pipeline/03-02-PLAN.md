---
phase: 03-ci-validation-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: []
autonomous: false
requirements: [CIVAL-01, CIVAL-02, CIVAL-03]

must_haves:
  truths:
    - "GitHub secret OPENCODE_API_KEY is configured in the repository"
    - "Opening a PR triggers the workflow and all four steps run"
    - "All four validation steps pass on a valid marketplace.json"
  artifacts:
    - path: "GitHub repo settings"
      provides: "OPENCODE_API_KEY secret for Claude Code CLI authentication"
  key_links:
    - from: "GitHub repo Settings > Secrets"
      to: ".github/workflows/validate.yml"
      via: "${{ secrets.OPENCODE_API_KEY }} in settings.json heredoc"
      pattern: "OPENCODE_API_KEY"
---

## Phase Goal

**As a** plugin author or maintainer, **I want to** have my PR automatically validated against the marketplace schema and plugin source requirements, **so that** only valid, well-formed marketplace entries can be merged.

<objective>
End-to-end verification of the CI pipeline. Configure the required GitHub secret, open a test PR, and confirm all four validation steps execute and pass.

Purpose: The workflow file exists but cannot be verified until a PR is opened and the GitHub secret is configured. This plan bridges the gap between "code written" and "CI actually works."
Output: Verified CI pipeline with all steps passing
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/cc-mplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/cc-mplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ci-validation-pipeline/03-01-SUMMARY.md
@.github/workflows/validate.yml
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Configure OPENCODE_API_KEY GitHub secret</name>
  <files></files>
  <action>Human must configure the OPENCODE_API_KEY secret in the GitHub repository. Go to https://github.com/Djarvur/cc-mplace, navigate to Settings then Secrets and variables then Actions, click New repository secret, name it OPENCODE_API_KEY, paste the opencode.ai API key for deepseek-v4-flash via https://opencode.ai/zen/go, and click Add secret. This is required for the Claude Code CLI validation step (per D-06). Claude cannot set repo secrets via CLI or API.</action>
  <verify>Secret OPENCODE_API_KEY appears in repo Settings > Secrets and variables > Actions list</verify>
  <done>OPENCODE_API_KEY secret is stored in GitHub repo settings and available to the workflow</done>
  <resume-signal>Type "done" once the OPENCODE_API_KEY secret is configured</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Open test PR and verify all four CI steps pass</name>
  <files>.github/workflows/validate.yml</files>
  <read_first>
    - .github/workflows/validate.yml (the workflow to verify)
  </read_first>
  <action>Push the current branch to GitHub if not already pushed. Open a test PR using gh pr create with title "test: verify CI validation pipeline" targeting main. Watch the Validate Marketplace workflow run in the Actions tab. Verify all four steps execute: Check formatting (Prettier), Run tests (Vitest), Validate with Claude Code (claude plugin validate --strict), Verify plugin sources (git ls-remote + GitHub API + raw URL HEAD). If all steps pass, close the test PR. If any step fails, examine logs and report the failure.</action>
  <verify>GitHub Actions workflow "Validate Marketplace" shows green checkmark on the test PR with all four steps passing</verify>
  <done>All four CI validation steps pass on the test PR: Prettier format check, Vitest tests, Claude Code validate, source repo verification</done>
  <resume-signal>Type "approved" if all four CI steps pass, or describe any failures with log output</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human -> GitHub repo settings | Secret configuration is a manual trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information Disclosure | API key visible to person configuring it | accept | Secret is only visible to repo admins; stored as GitHub Actions secret (encrypted at rest) |
| T-03-SC | Tampering | @anthropic-ai/claude-code npm package verified in CI run | mitigate | First CI run confirms package installs from official npm registry; verify publisher is "anthropics" |

Note: Primary threats (T-03-01 through T-03-05) are addressed in Plan 01. This plan covers the human-action trust boundaries for secret configuration and first-run verification.
</threat_model>

<verification>
1. OPENCODE_API_KEY secret exists in repo settings (confirmed by human)
2. Test PR opened and workflow triggered
3. All four steps pass (Prettier, Vitest, Claude Code validate, source verification)
4. Test PR closed after verification
</verification>

<success_criteria>
- OPENCODE_API_KEY secret configured in GitHub repo settings
- Test PR triggers the Validate Marketplace workflow
- All four validation steps pass
- CI pipeline is fully operational for future PRs
</success_criteria>

<output>
Create `.planning/phases/03-ci-validation-pipeline/03-02-SUMMARY.md` when done
</output>
